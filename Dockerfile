# PreciOps AI: Unable to determine the application type from the repository summary.
# Please specify a base image and the necessary commands to build and run your application.
# --- Example for a generic web server ---
# FROM nginx:alpine
# COPY . /usr/share/nginx/html
# EXPOSE 80
# CMD ["nginx", "-g", "daemon off;"]
